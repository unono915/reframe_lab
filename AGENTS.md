# AGENTS.md — 에이전트 작업 로그

> 이 저장소에서 AI 에이전트(Claude Code 등)가 수행한 작업의 **연속 기록**.
> `CLAUDE.md`가 "프로젝트를 어떻게 이해하고 무엇을 지켜야 하는가"라면,
> 이 파일은 "에이전트가 실제로 무엇을 했고 무엇을 발견했는가"의 시간순 로그다.

---

## 0. 이 파일의 사용 규칙

- **매 작업 세션마다 §3에 항목을 추가한다.** 날짜, 작업 내용, 발견한 문제, 검증 방법, 미완결 항목.
- **완료하지 못한 것을 완료로 적지 않는다.** 검증 수준(코드 검토 / 단위 테스트 / 실 DB·브라우저)을 반드시 명시한다.
- **단순 파일 수정 목록·명령어 나열은 적지 않는다.** 판단, 발견, 근거만 남긴다.
- `CLAUDE.md`와 중복되는 내용은 여기 반복하지 않고 참조만 한다.
- 이 파일은 git에 **포함된다** (`PRD.md`·`DESIGN.md`·`DEVELOPMENT_PLAN.md`·`CLAUDE.md`는 gitignore 대상이라 저장소에 없다).
  따라서 **기준 문서의 내용을 여기 복사하지 않는다** — 그 문서들은 비공개다.

## 1. 에이전트가 반드시 지켜야 하는 것

프로젝트 원칙 10개는 `CLAUDE.md` §3에 있다. 그중 에이전트가 코드를 쓸 때 가장 자주 어기게 되는 것:

1. **AI 관련 코드를 고칠 때 `src/lib/ai/guardrails.ts`를 우회하지 않는다.** 이 파일이 "AI가 사고를 대신하지 않는다"의 유일한 실행 지점이다.
2. **상태 전환·완료 조건은 `src/domain/training/`에만 둔다.** UI나 프롬프트에 조건을 복제하지 않는다.
3. **`NEXT_PUBLIC_` 접두사를 AI·비밀 키에 절대 붙이지 않는다.**
4. **`process.env[동적키]`로 `NEXT_PUBLIC_*`를 읽지 않는다.** 브라우저 번들에서 항상 undefined가 된다 (2026-08-26에 실제로 로그인을 막았던 버그).

## 2. 검증 수준의 정의

이 프로젝트는 **"단위 테스트 통과 = 완료"로 보지 않는다.** 아래 등급을 작업마다 명시한다.

| 등급 | 의미 |
|---|---|
| `L1 정적` | typecheck·lint·빌드 통과 |
| `L2 단위` | Unit Test 통과 |
| `L3 통합` | 실제 Supabase 프로젝트에 SQL로 결과 행을 직접 확인 |
| `L4 실동작` | 실제 브라우저로 사용자 경로를 끝까지 수행하며 확인 |

과거 세션에서 실제 버그(FK 삭제 차단, Guardrail 정규식 오탐, 로그인 차단)는 **전부 L3~L4에서만 발견됐다.** L2까지만 하고 완료로 보고하지 않는다.

---

## 3. 세션 로그

### 2026-09-01 — Upstage 연결 + 이론적 근거 검증

**A. Upstage `solar-pro4` 실제 제공자 연결 (§14-D 확정)** — `L1`/`L2` 완료, `L3`/`L4` **미완**

- `src/lib/ai/providers/upstage.ts` 신설, `getActiveCoachProvider()` 분기 교체. 키 없으면 Mock 폴백 유지.
- **모델 결정:** 사용자가 처음 제시한 `solar-mini`는 실제 API 호출 결과 `response_format: json_schema`를 지원하지 않아 프로젝트 요구사항(Structured Output 네이티브 강제)을 충족하지 못함을 확인 → `solar-pro4`로 변경 확정.
- **실측으로 발견한 제약:** enum 배열 필드에 `maxItems`가 없으면 모델이 같은 값을 무한 반복 생성해 JSON이 깨진다. 모든 배열에 상한 + `max_tokens` 캡 적용.
- **발견·수정한 버그 2건:**
  1. `coach`/`feedback` Route Handler의 재시도 루프가 provider의 예외(네트워크·타임아웃)를 처리하지 않아, 실패 시 규칙 기반 fallback 대신 raw 500이 나갈 수 있었다. → try/catch로 스키마 실패와 동일 처리.
  2. `src/lib/supabase/env.ts`가 `process.env[동적키]`로 `NEXT_PUBLIC_*`를 읽어 **브라우저 로그인이 항상 실패**하고 있었다. Next.js는 리터럴 표현만 빌드 시점에 치환한다. → `readSupabaseEnv()`로 리터럴 조회.
- **미완결:** Supabase 프로젝트가 `INACTIVE`(무료 티어 휴면)라 L3·L4 검증 불가. 사용자가 "재개하지 말고 마무리"를 선택. **다음 세션에서 프로젝트 재개 후 `provider='upstage'` 행이 실제로 남는지 반드시 확인할 것.**

**B. 병합 누락 발견 (기록과 실제 git 상태의 불일치)**

`CLAUDE.md`에 "phase-4-ai-coach를 main에 로컬 병합 완료"로 적혀 있었으나 `git merge-base --is-ancestor`로 확인한 결과 **사실이 아니었다.** `main`에는 Guardrail·Prompt·Context 격리 코드 전체가 없었다. 뒤늦게 병합(충돌 없음).

> **교훈:** 문서의 "병합했다"는 서술은 git 로그로 재확인하기 전까지 신뢰하지 않는다. 다른 문서 서술도 마찬가지다.

**C. 이론적 근거 검증 (연구 문헌 대조)** — 산출물: `RESEARCH_VALIDATION.md`

제품의 인과 사슬(문제 발견 훈련 → 문제 정의력 → 문제 해결력)을 7개 검증 가능한 고리로 분해해 연구 문헌과 대조. 결론 요약:

- **강하게 지지됨:** 문제 발견 능력의 실재성·예측력, 훈련 가능성, "AI가 프레임을 대신 만들지 않는다"는 핵심 설계 결정(CHI 2025 연구가 직접 지지).
- **구조적 결함 1건:** 생성(Generation) 단계만 있고 **통합(Consolidation) 단계가 없다.** Productive Failure 연구는 생성 후 전문가 해법과의 대조 분석이 반드시 뒤따라야 효과가 난다고 본다.
- **측정 타당도 문제:** `domain/growth/metrics.ts`의 지표가 전부 **횟수·빈도**다. PRD가 스스로 금지한 "단순 개수" 지표에 해당하며, 정의의 **품질 향상**을 측정하지 못한다.
- **명시적 교육 부재:** 온보딩에 개념 설명이 전혀 없다. 사용자는 '사실/해석/가정'을 배운 적 없이 분류하도록 요구받는다. 메타분석은 명시적 교육이 몰입형(immersion)보다 효과가 크다고 본다.

상세 근거·인용·개선안 우선순위는 `RESEARCH_VALIDATION.md` 참조.

### 2026-09-01 (이어서) — P0 개선안 4건 구현

`RESEARCH_VALIDATION.md` §5의 P0 개선안을 전부 구현했다. **검증 수준: `L1` + `L2`.
`L3`·`L4`는 Supabase가 `INACTIVE`라 미완** — 아래 §4-1이 최우선인 이유다.

| 항목 | 커밋 | 내용 |
|---|---|---|
| P0-3 명시적 교육 | `3935a07` | 단계별 "왜 이걸 하나요" 한 줄(`STAGE_RATIONALE`). 기본 접힘 |
| P0-2 루브릭 승격 | `10828a6` | 자기 점검을 AI 피드백보다 **먼저** 답하는 정규 단계로 |
| P0-4 변경 이유 | `10828a6` | v2 작성 경로 신설 + `changeReason` 필수 입력 |
| P0-1 통합 단계 | `e5d974d` | 렌즈별 대조 사례(`contrastive.ts`) — v1 저장 후 노출 |

**돌아보기 단계의 새 순서:** 생성(v1) → 통합(대조 사례) → 자기평가 → 외부 피드백·보정 → 수정(v2)

**구현 중 발견한 실버그 1건 (테스트로 잡음):**
> 자기 점검 키는 snake_case(`observation_evidence`), AI 차원 키는 camelCase(`evidence`)로
> **같은 6개 차원인데 이름 체계가 갈라져 있었다.** `dimensions[selfCheckKey]`가 항상
> undefined라 대조 기능 전체가 조용히 죽은 채 나갈 뻔했다 — 화면에는 "어긋난 항목 없음"이
> 늘 정상처럼 보였을, **실패가 눈에 띄지 않는 종류의 버그**다.
> 경계에 매핑을 두고, 두 키 집합이 다시 벌어지면 즉시 실패하는 테스트를 넣었다.

**함께 드러난 PRD 미준수 2건 (이번에 해소):**
- PRD §8 8단계가 요구하는 **v2 작성 경로가 아예 없었다** — 완료밖에 할 수 없었다.
- `changeReason`은 스키마·DB 컬럼·Result 표시까지 다 있었는데 **사용자에게 묻지를 않았다.**

**원칙 준수 확인:** P0-1의 대조 사례는 ① 사용자 문장이 아닌 남의 사례이고 ② AI가 생성하지
않는 고정 콘텐츠이며 ③ v1 저장 후에만 노출된다 — 원칙 3·User-first gate·Guardrail 어디에도
걸리지 않는다. 이 세 조건 중 하나라도 깨면 원칙 위반이 되므로 수정 시 반드시 유지할 것.

**검수 대기:** `contrastive.ts`의 8개 사례 문구는 `DAILY_TEMPLATES`와 같은 §14-F 검수 대상 초안이다.

### 2026-09-01 (이어서) — L3·L4 실동작 검증 완료

Supabase 프로젝트를 재개하고 **실제 DB·브라우저로 전 경로를 완주**했다. 검증 수준 `L4`.

> ⚠️ **재개 직후 판단 착오 기록.** 프로젝트가 `COMING_UP`인 동안 쿼리가 빈 결과를 돌려줘
> "테이블 0개·사용자 0명 — 데이터 전부 소실"로 사용자에게 잘못 보고했다. 기동이 끝난 뒤
> 확인하니 13개 테이블·세션 35개·사용자 1명이 **전부 온전했다.**
> **교훈: `COMING_UP` 상태의 빈 응답을 데이터 부재로 해석하지 않는다.** 상태가
> `ACTIVE_HEALTHY`가 될 때까지 기다린 뒤에 판단할 것. (마이그레이션은 적용되지 않았다 —
> 첫 시도가 "already exists"로 거부돼 아무것도 덮어쓰지 않았다.)

**검증한 것 (전부 실제 DB 행으로 확인):**

| 항목 | 결과 |
|---|---|
| Supabase 브라우저 로그인 | ✅ `env.ts` 수정이 실동작으로 확인됨 (지난 세션 미완 항목) |
| Upstage 연결 | ✅ `coach_interactions`·`ai_feedbacks` 모두 `provider='upstage'`, `model='solar-pro4'`, `status='ok'` |
| P0-3 단계별 설명 | ✅ 관찰 단계에서 펼쳐 문구 확인 |
| P0-1 통합 단계 | ✅ `goal_mismatch` 렌즈에 맞는 대조 사례가 v1 저장 후 정확히 노출 |
| P0-2 자기 점검 승격 | ✅ `stage_responses`에 `self_check_*` 6행 + 완료 표시 저장. 자기평가 전에는 AI 피드백 버튼이 열리지 않음 |
| P0-2 대조표 | ✅ **6개를 일부러 전부 '드러남'으로 과신 응답 → 코치가 `explore_further` 판정한 2개만 정확히 짚어냄.** 키 매핑 수정이 실제로 작동함을 확인 |
| P0-4 v2 + 변경 이유 | ✅ 이유를 비우면 검증에 걸리고, 채우면 `change_reason` 컬럼에 저장. Result 화면에 "바꾼 이유"로 표시 |

**이 검증에서 발견·수정한 실버그 1건 (커밋 `d32e792`):**
> Upstage가 `action:"ask"`인데 `question:null`을 반환했고 Guardrail이 통과시켰다.
> Route Handler는 `question`만 돌려주므로 **AI 호출은 소진되고(비용 발생) 힌트 버튼은
> 아무 반응 없이 끝났다.** 단위 테스트로는 잡히지 않는, 실패가 조용한 종류다.
> `checkAskHasQuestion` 추가 후 같은 버튼이 재시도를 거쳐 실제 질문을 반환하는 것을
> 라이브로 재확인했다.

> **패턴 주의:** 이 프로젝트에서 "힌트 버튼이 침묵한다"는 증상이 **세 번째**다
> (① Mock 시절 오류 무반응 ② User-first gate 오류 코드 ③ 이번 null 질문).
> 힌트·피드백 경로를 건드릴 때는 **모든 종료 경로에서 사용자에게 무엇이든 보이는지**를
> 반드시 확인할 것.

### 2026-09-02 — P1 개선안 + UI/UX 고도화

측정 타당도(이론)와 화면(UI/UX)을 함께 올렸다. 검증 수준 `L4`(실제 DB·브라우저).

**이론 — 측정이 실제 향상을 반영하도록**

| 항목 | 내용 |
|---|---|
| P1-5 품질 지표 | Growth 6개 지표가 전부 횟수·빈도였다(PRD §2.4가 스스로 금지한 것). 차원 충족도·자기 보정·힌트 의존도 **추이**로 교체하고, "생각이 달라진 지점"을 화면 맨 위로 올렸다. 표본 4개 미만이면 추세를 말하지 않는다 |
| P1-6 전이 프로브 | "오늘은 코치 없이 해보기". 켜면 힌트·피드백 UI가 사라지고 자기 점검만으로 완주한다. PRD §1.6-7("AI 없이도 향상")을 관측할 유일한 수단 |
| P1-8 간격 두고 다시 보기 | 14일 이상 지난 기록을 홈에서 제안. 분산 인출 원리 (PRD §6.5 P1) |

> ⚠️ **P1-6에서 잡은 허위 신호.** 처음에는 `aiCallCount === 0`만 보고 셌더니 Growth에
> "혼자 해낸 기록 **18번**"이 떴다 — 제공자가 연결되기 전의 과거 기록까지 센 것이다.
> **우연히 AI를 안 쓴 것과 스스로 안 쓰기로 한 것은 전혀 다른 증거다.** 사용자의 명시적
> 선택 표식을 요구하도록 고쳐 1번(실제)으로 정정됐다. 지표를 새로 만들 때는 "이 숫자가
> 과거 데이터에서 어떻게 계산되는가"를 반드시 실제 데이터로 확인할 것.

**UI/UX**

- **자기 점검 접근성** — 라디오 동그라미가 20px로 DESIGN.md의 최소 44×44px에 미달했고,
  legend가 fieldset 밖으로 빠져 질문과 선택지가 분리돼 보였다. 세그먼트(147×44px)로 바꾸고
  legend를 패널 안으로 넣었다.
- **대비 미달 자체 수정** — 선택 상태를 처음엔 solid brand + 흰 글자로 만들었는데 14px에서
  약 4.1:1로 **WCAG AA(4.5:1) 미달**이었다. Primary Button과 같은 조합(brand-soft +
  brand-strong) + Border로 바꿔, 색 하나가 아니라 테두리·굵기와 함께 전달하게 했다.
- **남은 요건 표시** — 각 단계는 버튼을 눌러야만 "질문 3개 이상" 같은 메시지를 냈다.
  이제 누르기 전에 실시간으로 알려준다("질문 2개, 핵심 질문 고르기가 남았어요").
- **Growth 리듬 차트** — 한 주 이상치(35번)가 나머지를 1px로 뭉갰고 0인 주도 채워진
  막대라 활동이 있는 것처럼 보였다. 두 번째로 큰 값 기준 + 높이 상한 + 빈 주는 점선 +
  주 라벨. (기준만 바꾸고 높이를 안 자르면 막대가 980px로 폭주한다 — 실제로 겪었다.)
- **온보딩** — 개념 설명 0줄인 스플래시 한 장이었다. DESIGN.md §10.1 규격대로 3단계
  (진행 Dot + 핵심 문구 3개)로 구현. **개인정보·AI 전송 문구는 §14-G 미결이라 넣지 않았다** —
  검토되지 않은 개인정보 문구를 사용자에게 보여줄 수 없다.

> **도구 메모.** 이 임베디드 브라우저는 HMR 직후 스크린샷이 타일처럼 깨져 보일 때가 있고,
> `getComputedStyle`도 실제 렌더와 다른 값을 돌려준 적이 있다(선택 상태를 "적용 안 됨"으로
> 오판할 뻔했다). **레이아웃 판단은 `getBoundingClientRect`·`scrollWidth` 같은 기하 값과
> 새로고침 뒤 스크린샷으로 교차 확인할 것.**

---

## 4. 다음 에이전트가 이어서 할 일

1. **P1-7(영역 고정)** — 사고 기술은 영역 지식에 의존하고(Willingham), 효과적 창의성 훈련은
   "영역에 적합한 현실적 연습"을 쓴다(Scott et al.). 사용자가 생활 영역 하나를 골라 그 안에서
   누적하게 하면 같은 노력으로 더 큰 효과를 기대할 수 있다. **MVP 범위 확대라 사용자 승인이 필요하다.**
2. **`contrastive.ts` 8개 사례 문구 검수**(§14-F, `DAILY_TEMPLATES`와 동일 대상).
3. **§14-G 결정 후 온보딩에 데이터·AI 전송 안내 화면 추가**(PRD F-01 요구사항 중 유일한 미구현분).
4. Phase 6(PWA 안정화) 중 배포·실기 테스트 없이 가능한 부분.
5. **Supabase는 무료 티어라 다시 휴면된다.** 검증이 필요한 세션은 `get_project`로 상태를 먼저
   확인하고, `COMING_UP`이면 `ACTIVE_HEALTHY`가 될 때까지 기다린 뒤 판단할 것.
